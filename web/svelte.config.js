// Minimal config so svelte-check has a config source.
// (Its vite-config loader looks for a plugin name that
// @sveltejs/vite-plugin-svelte v5 no longer registers.)
// All real options live inline in vite.config.ts.
export default {};
